# نسب — سجل إضافات وتعديلات Grok

التاريخ: 2026-08-30  
المشروع: تطبيق شجرة عائلة عربية **نسب**  
الصيغة: **ليس Flutter**. هذا مشروع ويب PWA (React + TypeScript + Vite) يعمل في المتصفح وعلى الماك/الآيفون كصفحة ويب. طلب الأصل كان Flutter أو React Native؛ التنفيذ داخل معاينة Grok كان ويبًا حتى تظهر الشجرة فورًا.

التشغيل بعد فك الضغط:

```bash
npm install
npm run dev
```

المنفذ: `8080`. الحفظ/الاستيراد: **GEDCOM 5.5.1** (`.ged`).

---

## 1) الطلب الأصلي

- شجرة بطاقات (Nodes) قابلة للتكبير والتصغير والتنقّل باللمس/الماوس.
- حقول: الاسم، الميلاد، الإقامة، وغيرها.
- حفظ محلي JSON باسم ديناميكي: التاريخ + الوقت + اسم آخر شخص.
- نافذة إضافة أبناء لا تُغلق بعد الحفظ؛ تُفرَّغ للحقل التالي حتى «إنهاء الإضافة».
- اتجاه عربي RTL.

## 2) قرارات البناء

- واجهة عربية موحدة: بطاقة ذكر كحلية، أنثى وردية، شجرة ساعة رملية (الوالدان يمينًا والأبناء يسارًا بعد طلب المستخدم).
- ثلاثة تبويبات: **الشجرة / البيوت / المروحة**.
- تخزين المسودة في `localStorage`، والصور والمستندات في IndexedDB.
- التبادل مع البرامج الأخرى: **GEDCOM 5.5.1** بدل JSON بعد إصرار المستخدم.
- الحفظ اليدوي فقط (لا حفظ صامت يزعج). اختيار مجلد النظام لا يعمل داخل إطار معاينة Grok؛ يعمل بعد Publish في كروم.

## 3) الإضافات حسب طلبات المستخدم (بالترتيب)

### واجهة الشجرة
- بطاقات مربعة/مستطيلة مع شارة الأحرف، العلم، الميلاد/الوفاة، الإقامة.
- تكبير/تصغير/ملاءمة، سحب اللوحة.
- نقرتان على الاسم تفتحان **ملف الشخص** (إضافة/حذف/تعديل من داخل البطاقة لا من الشريط فقط).
- شريط إجراءات سفلي كان معطوبًا ثم أُصلح (كان يعلّق على شخص واحد).

### البيوت والمروحة
- كل أسرة صغيرة = **بيت** برأس بيت اختياري.
- النقر على شخص في البيوت يفتح الشجرة مرتبة على محوره (إصلاح التبعثر).
- مخطط مروحة للأجداد.

### الجنس والقرابة
- إلغاء «غير محدد»؛ ذكر / أنثى فقط.
- أزرار: ابن، ابنة، أب، أم، أخ، أخت، زوج/زوجة.
- إضافة الابن ثم الابن ثم الابنة **دون إغلاق النافذة** (`حفظ وإضافة آخر` / `إنهاء الإضافة`).
- ربط كل ابن/ابنة بأمّه (زوجة مختارة).

### الزوجات
- حتى **أربع زوجات حاليات**؛ بعدها يُقفل زر الإضافة للحالية.
- زوجة سابقة ومتوفاة: مخفيّتان وتُجلبان عند الطلب (بدل كلمة مطلقة: **سابقة**).
- لا تُعرض خانة الزوجات عند إضافة بنت؛ تظهر عند إضافة زوجة.
- صفة الحالية لا تُفرض كأولى/ثانية في النموذج إلا من ترتيب الزوجات الفعلي.

### الصور والمستندات
- رفع من الكاميرا أو المكتبة.
- أحجام صغير / وسط / كبير داخل الإطار، وشريط زوم.
- تحريك الصورة في كل الاتجاهات لاختيار الوجه (`photoX` / `photoY`).
- مستندات قابلة للعرض والتكبير، وتسمية يدوية: شهادة ميلاد، دراسية، وفاة، صورة قبر، أخرى.

### القبر (اختياري ومخفي)
- مكان الدفن ورابط/إحداثيات GPS لا يظهران إلا عند الطلب حتى لا يُزعج من يشمئز.

### الدول
- قائمة بلدان مع علم على البطاقة.

### منع التكرار
- بحث اختياري عند الاسم يطابق الأب والأم عند تشابه الأسماء.

### الحفظ والاستيراد
- GEDCOM 5.5.1 كتابة وقراءة (`HEAD/INDI/FAM/SUBM`).
- زر حفظ، حفظ باسم، استيراد بسهم للأسفل «فتح ملف محفوظ».
- اسم الملف: الأساس عربي وامتداد `.ged` ثابت لاتجاه LTR حتى لا ينقلب إلى `ged.آل_المنصور`.
- معاينة Grok تمنع نافذة اختيار المستندات؛ المسودة تبقى في المتصفح. بعد Publish في كروم تظهر نافذة المجلد إن سمح المتصفح.
- نموذج تعبئة للعائلة: `/family-form.html` من أيقونة المشاركة أو لوحة الحفظ.

### بداية شجرة جديدة
- شجرة فارغة + نافذة **أول شخص** (الجد) بدل إجبار الشجرة التجريبية.
- الشجرة التجريبية لا تُستعاد فوق عمل المستخدم.

## 4) خريطة الملفات الأساسية

| مسار | الوظيفة |
|---|---|
| `src/components/nasab/tree-canvas.tsx` | لوحة الشجرة، زوم/سحب، الموصلات |
| `src/components/nasab/person-card.tsx` | بطاقة الفرد |
| `src/components/nasab/person-file.tsx` | ملف الشخص بالنقرة المزدوجة |
| `src/components/nasab/add-kin-dialog.tsx` | نافذة القرابة المستمرة |
| `src/components/nasab/houses-view.tsx` | البيوت |
| `src/components/nasab/fan-chart.tsx` | المروحة |
| `src/components/nasab/photo-adjust.tsx` | قص/تحريك الصورة |
| `src/lib/tree/store.ts` | حالة الشجرة (Zustand) |
| `src/lib/tree/gedcom.ts` | تصدير/استيراد GEDCOM 5.5.1 |
| `src/lib/tree/storage.ts` | localStorage والمسودة |
| `src/lib/tree/graph.ts` | الزوجات، البيوت، الأجيال |
| `src/lib/tree/layout.ts` | ترتيب الساعة الرملية |
| `src/lib/tree/media.ts` | IndexedDB للصور والملفات |
| `public/family-form.html` | نموذج يُرسل للأقارب لتعبئة بياناتهم |
| `src/lib/tree/copy.ts` | النصوص العربية |

## 5) حقول الشخص في البرنامج

الاسم، اسم الأب، العائلة، الجنس، الميلاد ومكانه، الوفاة ومكانها، الإقامة، المهنة، البلد، صورة (حجم/زوم/إزاحة)، مستندات، مكان الدفن، GPS، أب، أم، أزواج، صفة الزوجية، رأس بيت.

## 6) ما لم يُنجز بعد (صريح)

- تطبيق أصلي حقيقي عبر App Store / Google Play (Capacitor) — يحتاج ماك بإكس كود وحساب مطوّر أبل؛ **راجع `التثبيت-على-الأجهزة.md`** للخطوات التالية.
- مزامنة سحابية أو خادم (البيانات محلية فقط: `localStorage` + IndexedDB).

## 7) تسليم لابن المبرمج

هذا الأرشيف هو الـ **workspace**.  
`npm install` ثم `npm run dev`. دليل مختصر في `DEV` داخل الأرشيف إن وُجد، وإلا هذا الملف كافٍ.

## 8) تحويل إلى PWA قابل للتثبيت على ماك/آيفون/آيباد/أندرويد (Claude)

**التاريخ: 2026-08-30.** كان التطبيق ويب فقط (يعمل في المتصفح بدون تثبيت). أُضيفت
طبقة PWA كاملة حتى يُثبَّت كتطبيق حقيقي على الشاشة الرئيسية/الرصيف على الأجهزة
الأربعة، بدون تعديل أي منطق للشجرة أو GEDCOM:

- **أيقونات حقيقية بدل الافتراضية**: أُنشئت من `favicon.svg` (نفس شعار الشجرة
  ثلاثية العقد) بمقاسات 192 و512 (عادية وقابلة للقص "maskable") في
  `public/icons/`، واستُبدلت بها `public/__grok/icon-180.png` القديمة (كانت
  أيقونة عامة غير خاصة بالتطبيق).
- **`renderWebManifest`** في `scripts/grok-pwa-shared.mjs` كان يشتق الاسم من
  اسم المضيف (`*.grok.me`) ويرجع لاسم عام "Grok App" مع أيقونة سوداء واحدة على
  أي استضافة أخرى (Vercel، نطاق خاص، ...). صار الآن ثابتًا: الاسم "نسب"،
  الألوان بلون التطبيق (#F3EEE4)، ومجموعة الأيقونات كاملة — يعمل بشكل صحيح على
  أي استضافة.
- **`public/sw.js`** (جديد): خدمة عامل (Service Worker) بسيطة — الشبكة أولًا
  للتنقّل مع نسخة احتياطية من المخزن المؤقت (تشغيل بدون إنترنت)، و
  stale-while-revalidate للأصول الثابتة (JS/CSS/صور/خطوط). لا تُخزَّن مسارات
  `/api/`، `/auth/`، `/__grok/`. تُسجَّل من `src/components/pwa-register.tsx`
  (مثبَّت في `src/routes/__root.tsx` بجانب `PreviewHostBridge`).
- **وسوم `<meta>` في `__root.tsx`**: أُضيفت `apple-mobile-web-app-capable`،
  `apple-mobile-web-app-status-bar-style`، `apple-mobile-web-app-title`،
  `mobile-web-app-capable`، `application-name` — حتى يفتح التطبيق بواجهة كاملة
  بدون شريط سفاري على آيفون/آيباد. روابط `manifest` و`apple-touch-icon` بقيت
  تشير إلى `/__grok/manifest.webmanifest` و`/__grok/icon-180.png` (لا تكرار مع
  حَقن `grok-pwa-plugin`/`grok-pwa.ts`، الذي يتحقق من هذين المسارين حرفيًا قبل
  الحقن).
- **تحقّق فعلي (Playwright, headless Chromium)**: `npm run typecheck` و
  `npm run build` نظيفان، المانفست يُخدَّم بمحتواه الصحيح على `/dev`، الأيقونات
  والـ Service Worker يُخدَّمان (200)، لا وسوم `<link>`/`<meta>` مكرّرة في
  الصفحة المُصيَّرة، الـ Service Worker يسجَّل ويُفعَّل فعليًا، وإعادة تحميل
  الصفحة **أثناء قطع الشبكة (offline) تنجح وتعرض العنوان بشكل صحيح**.
- **لم يُغيَّر**: منطق الشجرة، GEDCOM، التخزين، auth/db (لا تزال بلا استخدام
  فعليًا كما كانت). لا حاجة لأي مفتاح بيئة جديد.

**الخطوة المتبقية على المستخدم**: نشر هذا الأرشيف على استضافة HTTPS حقيقية
(Vercel جاهز عبر `nitro` preset) — التثبيت الفعلي على آيفون/آيباد/أندرويد لا
يعمل من `localhost`. التفاصيل والخطوة بخطوة في **`التثبيت-على-الأجهزة.md`**.

## 9) تغذية الشجرة ببيانات حقيقية من مخطط شجرة الحمراني (Claude)

**التاريخ: 2026-08-31.** المستخدم رفع صورة مخطط "شجرة العائلة الحمراني"
(القطعة الأولى، 1435هـ/2014م) وطلب تغذية التطبيق بها. تم نسخ فرع واحد يدويًا
من المخطط (عبدالله بن علي، 1900-1985، وذريته حتى الجيل الرابع، 22 شخصًا)
والتحقق منه فعليًا عبر `fromGedcom` قبل اعتماده. بقية فروع المخطط (بوحمد،
الجعفر، الموسى، العمراني، وفرعان آخران) أكبر بكثير (يُقدَّر مجموع المخطط
كله بـ500-800 شخص) ولم تُنسخ بعد — تتابع لاحقًا فرعًا فرعًا.

- **`src/lib/tree/hamrani-seed.ts`** (جديد): `createHamraniSeedTree()` يبني
  بيانات الفرع المنسوخ كـ`TreeData` جاهزة، بنفس نمط `demo-data.ts`.
- **`src/lib/tree/store.ts`**: فرع `hydrate()` الذي كان يفتح شجرة فارغة +
  نافذة "أضف أول شخص" عند عدم وجود بيانات محفوظة، صار يحمّل
  `createHamraniSeedTree()` بدلًا منها (`showDemoBanner: true` بدل فتح نافذة
  إجبارية) — أول تشغيل بمتصفح جديد يفتح على الفرع المنسوخ مباشرة بدل شجرة
  فارغة. أي تعديل من المستخدم يُحفظ فورًا ولا يُستبدل مرة أخرى (نفس آلية
  الحماية الموجودة أصلًا لـ`createDemoTree`/`آل المنصور`).
- **`src/lib/tree/copy.ts`**: نص شريط "شجرة تجريبية" صار "فرع أولي من مخطط
  شجرة الحمراني — راجعه وأكمله، أو ابدأ من جديد" (الزر القديم `resetDemo`
  الذي يحمّل شجرة آل المنصور التجريبية لم يكن مربوطًا بأي واجهة أصلًا، فلم
  يتأثر أي مسار موجود).
- **تحقّق فعلي**: `npm run typecheck` و`npm run build` نظيفان، وشُغِّل
  التطبيق فعليًا (Playwright, headless Chromium) على متصفح بلا بيانات محفوظة
  مسبقًا — ظهرت شجرة "عبدالله الحمراني" بـ22 شخصًا و5 أجيال والشريط التوضيحي
  فورًا، مطابقة للقطة شاشة محفوظة.
- **لم يتغيّر**: أي مسار عندهم بيانات محفوظة مسبقًا في المتصفح (`localStorage`)
  — هؤلاء يستمرون على شجرتهم كما هي، هذا التغيير يخص فقط أول فتح بمتصفح/جهاز
  جديد لا يحمل بيانات محفوظة.

بيانات المصدر (JSON) وسجل تقدّم نسخ بقية فروع المخطط محفوظان في مشروع
"Family Tree" على claude.ai (`hamrani-tree-progress.md`,
`hamrani-branch1-ali-abdullah.json`) لمتابعة الفروع الباقية لاحقًا.
