# تثبيت «نسب» على الإنترنت وماك وآيفون/آيباد وأندرويد

هذا الملف مُحدَّث بعد إضافة **Capacitor** فعليًا للمشروع (مجلدا `ios/`
و`android/` جاهزان داخل أرشيف المشروع المُرسَل إليك). بما أن Xcode وVisual
Studio Code موجودان على جهاز الماك عندك، تقدر تكمل المسار الحقيقي (تطبيق
فعلي على ماك وآيفون/آيباد) مباشرة. أندرويد يحتاج Android Studio (مجاني من
Google) إن لم يكن مثبتًا عندك.

## ملخص المسارات الثلاثة

1. **الموقع على الإنترنت** — رابط https حقيقي عبر Vercel. مطلوب أولًا لأي
   مسار آخر.
2. **PWA سريع (بلا Xcode ولا Android Studio)** — تثبيت أيقونة حقيقية على كل
   جهاز مباشرة من المتصفح، خلال دقيقة. يعمل بدون إنترنت بعد أول فتح.
3. **تطبيق أصلي حقيقي عبر Xcode / Android Studio** — نفس كود الويب مُغلَّف
   بواسطة Capacitor داخل مشروع Xcode (آيفون/آيباد، وماك عبر Mac Catalyst)
   ومشروع Android Studio — قابل للتشغيل مباشرة على جهازك، وقابل للتوقيع
   والرفع لاحقًا إلى App Store / Google Play إن رغبت.

## الخطوة 1: انشر التطبيق على رابط عام (مطلوبة دائمًا)

لازم يكون على رابط `https://...` حقيقي — لن يعمل التثبيت من `localhost` على
جهاز آخر، ولا مسار Capacitor أدناه. المشروع مُجهَّز مسبقًا لهذا (إعداد
Nitro/vercel):

1. أنشئ حسابًا مجانيًا على vercel.com (تقدر تسجّل الدخول بحساب GitHub).
2. ارفع مجلد المشروع إلى مستودع GitHub جديد، ثم من لوحة Vercel: **Add New →
   Project**، اختر المستودع، اضغط **Deploy**.
   (بديل أسرع بدون GitHub: من داخل مجلد المشروع بعد `npm install`، شغّل
   `npx vercel --prod` واتبع التعليمات.)
3. بعد انتهاء النشر تحصل على رابط مثل `https://your-app.vercel.app` — احتفظ
   به، ستحتاجه في الخطوة 3.

## الخطوة 2: تثبيت PWA سريع على كل جهاز

### آيفون / آيباد (سفاري)
1. افتح الرابط في **Safari** (لازم سفاري تحديدًا، ليس كروم).
2. زر **المشاركة** (المربع وفوقه سهم) → **إضافة إلى الشاشة الرئيسية**.

### أندرويد (كروم)
1. افتح الرابط في **Chrome**.
2. من قائمة (⋮) اختر **تثبيت التطبيق** / **إضافة إلى الشاشة الرئيسية**
   (أو اضغط الإشعار التلقائي إن ظهر).

### ماك
- **سفاري 17 فما فوق**: من قائمة **ملف** اختر **إضافة إلى الرصيف**.
- **كروم/إيدج**: أيقونة التثبيت داخل شريط العنوان (يمين الرابط) → **تثبيت**.

أي تعديل تنشره لاحقًا (بعد إعادة النشر على Vercel) يصل تلقائيًا للأجهزة
المثبَّتة تلقائيًا — بلا حذف أو إعادة تثبيت.

## الخطوة 3: تطبيق أصلي حقيقي (Xcode + Android Studio)

المشروع فيه الآن `capacitor.config.ts` ومجلدا `ios/` و`android/` جاهزان.
بما أن التطبيق يُبنى كخادم (SSR) على Vercel وليس كموقع ثابت، جعلت Capacitor
يفتح **رابطك المنشور فعليًا** بدل نسخة محلية — يبقى يعمل بلا إنترنت بعد أول
فتح بفضل عامل الخدمة (Service Worker) المُفعَّل مسبقًا، وبياناتك تبقى محفوظة
محليًا على الجهاز كما هي الآن.

### أولًا (مرة واحدة، من الطرفية Terminal على جهاز الماك)

1. افتح مجلد المشروع في VS Code أو الطرفية.
2. عدّل ملف `capacitor.config.ts`: استبدل السطر
   `const DEPLOYED_URL = 'https://REPLACE-WITH-YOUR-VERCEL-URL.vercel.app';`
   برابطك الحقيقي من الخطوة 1.
3. شغّل:
   ```bash
   npm install
   npx cap sync
   ```

### آيفون / آيباد (وماك عبر Mac Catalyst)

```bash
npx cap open ios
```

يفتح Xcode تلقائيًا على مشروع `App`:
- **آيفون/آيباد**: اختر جهازك أو محاكيًا من قائمة الأجهزة أعلى النافذة، ثم
  ▶ تشغيل. لتشغيله فعليًا على آيفونك الشخصي أول مرة: من تبويب **Signing &
  Capabilities** اختر فريقك (حسابك الشخصي في Apple ID يكفي للتجربة على
  جهازك، بدون اشتراك المطوّرين المدفوع).
- **ماك (Mac Catalyst)**: في نفس تبويب **Signing & Capabilities** اضغط زر
  **+ Capability** إن لزم، وتأكد أن **Mac Catalyst** مفعّل ضمن "Supported
  Destinations" (أعلى الصفحة العامة للـ target). بعدها يظهر خيار **My Mac**
  في قائمة الأجهزة — اختره واضغط ▶، فيشتغل التطبيق كتطبيق ماك حقيقي منفصل
  (نافذة، أيقونة Dock، Launchpad) بدون أي متصفح ظاهر.

النشر لاحقًا على App Store يحتاج حساب Apple Developer (99$/سنة) — غير مطلوب
للتجربة والاستخدام الشخصي على جهازك.

### أندرويد

يحتاج **Android Studio** (مجاني، من developer.android.com/studio) إن لم يكن
مثبتًا عندك.

```bash
npx cap open android
```

يفتح Android Studio على مشروع `android/`:
- **تجربة سريعة**: شغّل على محاكي (Emulator) أو جهاز أندرويد حقيقي موصول
  بالـ USB (مع تفعيل "خيارات المطوّر" و"تصحيح USB" على الجهاز)، بزر ▶.
- **APK حقيقي للتوزيع**: من القائمة **Build → Generate Signed Bundle / APK**،
  اتبع المعالج لإنشاء مفتاح توقيع (Keystore) — يُنتج ملف `.apk` أو `.aab`
  قابل للتثبيت مباشرة أو الرفع إلى Google Play (حساب مطوّر مرة واحدة بـ 25$).

### ملاحظة

كل مرة تنشر فيها تحديثًا جديدًا على Vercel، لا تحتاج إعادة بناء تطبيقي
Xcode/Android — لأنهما يفتحان الرابط الحي مباشرة، فالتحديث يصل تلقائيًا.
تحتاج إعادة `npx cap sync` وإعادة بناء فقط لو غيّرت `capacitor.config.ts`
نفسه (مثل تغيير الرابط) أو أضفت إضافات Capacitor جديدة.
